---
title: Realization of a Fully Connected Neural Layer Over-the-Air through Multi-hop Amplify-and-Forward Relays
title_zh: 通过多跳放大转发中继在空中实现全连接神经层
authors: "Tolga Girici, Meng Hua, Deniz Gündüz"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.20489v1"
tags: ["query:dl-comm"]
score: 10.0
evidence: 通过无线中继实现空中神经网络层
tldr: 本研究探讨了利用无线空中计算（AirComp）实现神经网络全连接层的方法。针对包含多天线收发机及多个多跳放大转发（AF）中继节点的系统，提出了一种优化框架。通过交替优化发射机预编码器、接收机合并器和中继增益，在满足功率约束的同时提高计算精度。仿真结果表明，该多跳中继方案在神经网络分类任务中能达到近乎完美的准确率，为分布式智能计算提供了高效的无线实现方案。
selection_source: fresh_fetch
motivation: 旨在利用无线空中计算技术在多跳中继网络中高效实现神经网络的全连接层计算。
method: 提出一种交替优化框架，协同优化发射机预编码、接收机合并及中继放大增益，以最小化计算误差。
result: 仿真实验证明，该多跳中继系统在神经网络应用中能够实现极高的分类准确率。
conclusion: 多跳放大转发中继技术能有效支持无线空中计算，为实现高性能的分布式神经网络层提供了可行路径。
---

## 摘要
我们研究了利用无线空中计算实现神经网络全连接层的问题。我们假设了一个包含多天线发射机和接收机，以及中间若干多跳放大转发中继设备的多跳系统。我们构建了一个优化问题，在满足中继设备功率约束和发射机功率约束的条件下，优化发射机预编码器、接收机合并器以及放大转发增益。我们提出了一个旨在优化模仿准确度的交替优化框架。仿真研究结果表明，在神经网络中使用多跳中继可以实现近乎完美的分类准确率。

## Abstract
We study the problem of implementing a fully-connected layer of a neural network using wireless over-the-air computing. We assume a multi hop system with a multi-antenna transmitter and receiver, along with a number of multi-hop amplify-and-forward relay devices in between. We formulate an optimization problem that optimizes the transmitter precoder, receiver combiner and amplify-and-forward gains, subject to relay device power constraint and transmitter power constraint. We propose an alternating optimization framework that optimizes the imitation accuracy. Simulation study results reveal that multi-hop relaying achieves an almost perfect classification accuracy when used in a neural network.

---

## 论文详细总结（自动生成）

以下是对论文《Realization of a Fully Connected Neural Layer Over-the-Air through Multi-hop Amplify-and-Forward Relays》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在无线通信中利用“空中计算”（Over-the-Air Computation, AirComp）技术，通过多跳放大转发（AF）中继物理性地实现神经网络的全连接层（FC Layer）。
*   **研究动机**：
    *   **边缘计算需求**：现代边缘应用（如AR/VR、自动驾驶）对推理延迟和能效要求极高，传统的数字推理在云端会产生高延迟和重负载。
    *   **空中计算优势**：利用无线信道的叠加特性直接完成矩阵乘加运算，避免了量化、封包和重建的开销。
    *   **多跳中继必要性**：现有的空中计算研究多集中于单跳或RIS（可重构智能表面）。但在长距离传输、直连信道受阻或信道秩亏（Rank Deficient）的情况下，需要多跳中继来增强信号质量并提升等效信道的秩。

### 2. 方法论
*   **核心思想**：通过协同优化发射机预编码器、接收机合并器以及中间各跳中继的放大增益，使整个无线物理信道的等效矩阵（Effective Channel）尽可能逼近预训练好的神经网络全连接层权重矩阵 $W$。
*   **系统模型**：
    *   包含一个多天线基站（BS）、一个多天线接收机（Rx）以及分布在 $L$ 个中继组中的 $K$ 个单天线放大转发（AF）中继。
    *   信号流：BS $\rightarrow$ 中继组1 $\rightarrow$ ... $\rightarrow$ 中继组L $\rightarrow$ Rx。
*   **关键技术细节**：
    *   **优化目标**：最小化“模仿误差”（等效信道与目标权重的 Frobenius 范数差）以及“噪声传播项”（防止噪声被过度放大）。
    *   **约束条件**：基站总发射功率约束和每个中继节点的个体功率约束。
*   **算法流程（交替优化 - AO）**：
    1.  **更新基站预编码器 $F_1$**：固定其他参数，通过拉格朗日乘子法和二分查找解决受限最小二乘问题。
    2.  **更新接收合并器 $F_2$**：固定其他参数，利用闭式解（类似维纳滤波）直接更新。
    3.  **更新中继增益 $A_l$**：利用向量化和 Khatri-Rao 积将多跳级联关系展开，引入“噪声感知正则化”来抑制噪声敏感性，并进行功率投影以满足硬件约束。

### 3. 实验设计
*   **数据集**：使用 **Fashion-MNIST** 进行图像分类任务验证。
*   **Benchmark（基准）**：理想状态下的**数字基线（Digital Baseline）**，即不考虑无线传输损耗的纯软件神经网络准确率（84.5%）。
*   **对比场景**：
    *   不同中继组数（$L=1, 2, 3$）。
    *   不同中继节点数量（$K$ 从 6 到 60 不等）。
    *   不同功率预算（$P_k = 1W, 0.1W, 0.01W$）。
    *   直连信道（BS-Rx）存在与否的对比。
    *   不同网络覆盖直径（100米 vs 200米）。

### 4. 资源与算力
*   **算力说明**：论文中**未明确提到**具体的 GPU 型号、数量或训练时长。由于该研究侧重于通信物理层的仿真（基于 MATLAB 或 Python 的数值模拟），其算力需求主要集中在矩阵运算和交替优化算法的迭代上，而非大规模深度学习训练。

### 5. 实验数量与充分性
*   **实验规模**：每个数据点均基于 **20 次独立的信道实现**取平均值，并绘制了误差棒（Error Bars）以显示性能波动。
*   **充分性**：实验覆盖了功率、距离、中继密度和跳数等关键变量，验证了算法在不同环境下的鲁棒性。
*   **客观性**：通过引入噪声感知正则化和实际的路径损耗模型（3GPP UMi 标准），实验环境较为贴近真实物理场景。

### 6. 主要结论与发现
*   **高精度模仿**：在功率充足的情况下，多跳 AirFC 可以达到与数字基线几乎一致的分类准确率（约 84%）。
*   **多跳优势**：在长距离（200米）场景下，单跳中继效果极差，而增加到 2 跳或 3 跳能显著提升准确率，因为缩短了单跳距离并降低了路径损耗。
*   **边际递减**：跳数增加带来的收益会逐渐饱和，因为每一跳都会引入新的热噪声累积。
*   **功率敏感性**：中继功率对性能至关重要，当功率降至 0.01W 时，系统性能会发生剧烈抖动且准确率大幅下降。

### 7. 优点
*   **解决秩亏问题**：通过分布式中继有效提升了等效信道的秩，使得在毫米波等稀疏信道下也能实现高维矩阵运算。
*   **算法鲁棒性**：提出的交替优化算法包含噪声抑制机制，避免了中继增益爆炸问题。
*   **灵活性**：该方案不依赖于昂贵的 RIS 部署，利用现有的 AF 中继即可实现。

### 8. 不足与局限
*   **理想化假设**：假设拥有**完美的信道状态信息（CSI）**，未讨论信道估计误差对计算精度的影响。
*   **同步问题**：未考虑多个中继节点在空中叠加信号时的符号同步和载波频率偏移（CFO）问题。
*   **能效考量**：虽然降低了推理延迟，但多个中继节点同时工作的总能耗未与传统方案进行深度对比。
*   **应用限制**：目前仅验证了全连接层，对于卷积层或其他复杂结构的空中实现尚需进一步扩展。

（完）
