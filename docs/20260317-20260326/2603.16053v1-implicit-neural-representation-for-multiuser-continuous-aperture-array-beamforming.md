---
title: Implicit Neural Representation for Multiuser Continuous Aperture Array Beamforming
title_zh: 面向多用户连续孔径阵列波束赋形的隐式神经表示
authors: "Shiyong Chen, Jia Guo, Shengqian Han"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.16053v1"
tags: ["query:dl-comm"]
score: 9.0
evidence: 用于连续孔径阵列波束赋形的隐式神经网络表示
tldr: 针对连续孔径阵列（CAPA）多用户波束赋形中隐式神经表示（INR）训练复杂且泛化性差的问题，本文推导了多用户多CAPA系统的和速率闭式解，并提出了一种基于函数式WMMSE算法和图神经网络（GNN）的BeamINR方法。该方法利用置换等变性结构，在显著降低推理延迟和训练复杂度的同时，提升了在不同用户数和载波频率下的泛化性能。
selection_source: fresh_fetch
motivation: 现有的基于INR的CAPA波束赋形方法面临训练复杂度高和对用户数量、频率等参数泛化能力有限的挑战。
method: 提出BeamINR方法，结合函数式WMMSE算法的迭代结构与图神经网络，利用正交基展开将泛函优化转化为参数优化。
result: 实验表明，BeamINR在保持较高和速率的同时，大幅降低了推理延迟和训练成本，并展现出优异的跨场景泛化能力。
conclusion: 本研究证明了结合数学优化结构与深度学习模型能有效提升连续孔径阵列在大规模多用户通信中的实时波束赋形性能。
---

## 摘要
隐式神经表示 (INR) 可以参数化连续孔径阵列 (CAPA) 中的连续波束赋形函数，从而实现高效的在线推理。然而，现有的基于 INR 的 CAPA 波束赋形方法通常面临训练复杂度高和泛化能力有限的问题。为了解决这些问题，我们首先推导了基站和用户均配备 CAPA 的多用户多 CAPA 系统中可实现和速率的闭式表达式。针对和速率最大化问题，我们利用正交基展开将泛函优化转换为等效的参数优化问题，进而开发了一种泛函加权最小均方误差 (WMMSE) 算法。基于该泛函 WMMSE 算法，我们进一步提出了 BeamINR，这是一种利用图神经网络实现的基于 INR 的波束赋形方法，旨在利用最优波束赋形策略的置换等变结构；其更新方程是根据泛函 WMMSE 迭代结构设计的。仿真结果表明，泛函 WMMSE 算法以较高的在线复杂度为代价实现了最高的和速率。与基准 INR 相比，BeamINR 显著降低了推理延迟和训练复杂度，并且在不同用户数量和载波频率下具有更好的泛化性能。

## Abstract
Implicit neural representations (INRs) can parameterize continuous beamforming functions in continuous aperture arrays (CAPAs) and thus enable efficient online inference. Existing INR-based beamforming methods for CAPAs, however, typically suffer from high training complexity and limited generalizability. To address these issues, we first derive a closed-form expression for the achievable sum rate in multiuser multi-CAPA systems where both the base station and the users are equipped with CAPAs. For sum-rate maximization, we then develop a functional weighted minimum mean-squared error (WMMSE) algorithm by using orthonormal basis expansion to convert the functional optimization into an equivalent parameter optimization problem. Based on this functional WMMSE algorithm, we further propose BeamINR, an INR-based beamforming method implemented with a graph neural network to exploit the permutation-equivariant structure of the optimal beamforming policy; its update equation is designed from the structure of the functional WMMSE iterations. Simulation results show that the functional WMMSE algorithm achieves the highest sum rate at the cost of high online complexity. Compared with baseline INRs, BeamINR substantially reduces inference latency, lowers training complexity, and generalizes better across the number of users and carrier frequency.

---

## 论文详细总结（自动生成）

这篇论文针对下一代无线通信中的**连续孔径阵列（CAPA）**波束赋形问题，提出了一种结合数学优化结构与深度学习的新型方案。以下是对该论文的结构化深度总结：

### 1. 核心问题与研究动机
*   **背景**：随着全息 MIMO 技术的发展，连续孔径阵列（CAPA）被视为天线密度趋于无穷大的极限情况。与传统离散阵列不同，CAPA 的波束赋形设计涉及**连续电流分布函数**的优化，属于无穷维泛函优化问题。
*   **核心问题**：
    1.  **计算复杂度高**：传统的数值方法（如变分法或傅里叶展开）涉及复杂的迭代积分，难以满足实时通信需求。
    2.  **学习效率低**：现有的隐式神经表示（INR）方法多采用全连接网络（FNN），训练复杂度高，且对用户数量、载波频率等系统参数的**泛化能力极差**。
*   **动机**：设计一种既能利用数学优化算法的结构化优势，又能发挥神经网络快速推理能力的波束赋形方案。

### 2. 方法论
论文提出了 **BeamINR** 框架，其核心步骤如下：
*   **理论推导**：首次推导了基站和用户均配备 CAPA 的多用户系统中**可实现和速率的闭式表达式**，并利用泛函分析工具证明了其等效的 MSE 最小化形式。
*   **泛函 WMMSE 算法**：
    *   利用**正交基展开（Orthonormal Basis Expansion）**将泛函优化问题转化为参数优化。
    *   推导出波束赋形函数 $v(s)$ 和合并函数 $u(r)$ 的闭式更新方程，避免了离散化带来的近似损失。
*   **BeamINR 模型设计**：
    *   **架构**：采用图神经网络（GNN）实现 INR，利用其**置换等变性（Permutation Equivariance）**处理多用户场景。
    *   **更新规则**：不同于标准 GNN，BeamINR 的层间更新方程是根据泛函 WMMSE 的迭代结构设计的，将信道核函数（Channel Kernel）的聚合与组合逻辑嵌入网络层。
*   **训练策略**：采用无监督学习，结合**高斯-勒让德（GL）正交规则**和**随机索博尔（Sobol）采样**进行混合训练，以增强模型在连续孔径空间上的平滑度和泛化性。

### 3. 实验设计
*   **场景设置**：下行多用户多 CAPA 系统，载波频率 2.4GHz（及 1.8-3GHz 泛函测试），基站孔径 2m×2m，用户孔径 0.5m×0.5m。
*   **基准方法（Benchmarks）**：
    1.  **Functional WMMSE**：本文提出的数值优化算法（作为性能上限）。
    2.  **ConINR**：采用标准 GNN 更新规则的 INR。
    3.  **VarINR**：BeamINR 的变体（不利用图拓扑）。
    4.  **Fourier**：基于傅里叶基展开的传统离散化方法。
    5.  **SPDA**：基于空间离散阵列近似的方法。
*   **评估维度**：和速率、推理延迟、训练复杂度、跨用户数泛化、跨频率泛化、跨孔径尺寸泛化。

### 4. 资源与算力
*   **训练细节**：使用 Adam 优化器，初始学习率 $10^{-3}$，Batch Size 为 32。
*   **数据量**：训练集包含 50 万个样本。
*   **算力开销**：
    *   **BeamINR 训练时间**：约 **1.62 小时**（达到 95% 性能水平所需时间）。
    *   **模型规模**：参数量约为 **3.68 M**。
    *   **硬件说明**：文中未明确指出具体的 GPU 型号（如 A100 或 RTX 4090），但给出了具体的推理耗时（BeamINR 为 0.052s/样本）。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度的仿真，包括不同电流预算（Current Budget）、用户数量（2-8人）、孔径大小及频率范围。
*   **充分性**：实验设计较为全面。特别是**泛化性实验**（在 K=5 时训练，在 K=2~8 时测试）有力地证明了模型驱动架构的优势。
*   **客观性**：通过与数值优化上限（Functional WMMSE）的对比，客观展示了深度学习方法的性能损失与增益平衡。

### 6. 主要结论
*   **性能**：泛函 WMMSE 算法达到最高和速率，但推理极慢（1.378s）。
*   **效率**：BeamINR 的推理速度比数值算法快约 **26 倍**，且在训练样本需求和训练时长上远优于传统 INR（ConINR）。
*   **泛化性**：BeamINR 在改变用户数量和载波频率时，性能下降远小于基准网络，证明了将数学模型嵌入网络结构的有效性。

### 7. 优点
*   **模型驱动的创新**：不是简单地将神经网络当作黑盒，而是通过泛函 WMMSE 的迭代逻辑指导 GNN 的层设计，具有很强的物理可解释性。
*   **泛函域处理**：直接在连续域定义更新规则，避免了传统方法中因傅里叶截断或空间采样导致的精度损失。
*   **泛化能力强**：成功解决了 INR 在无线通信应用中常见的“场景耦合”问题，使其具备了一定的实际部署潜力。

### 8. 不足与局限
*   **信道模型简化**：实验主要基于视距（LoS）信道模型，对于复杂的非视距（NLoS）或多径衰落环境的适应性有待验证。
*   **计算开销**：虽然推理比数值方法快，但 BeamINR 的更新方程涉及积分近似，其推理延迟（0.052s）仍高于纯数据驱动的简单 FNN（0.033s）。
*   **硬件实现**：论文讨论的是理想的连续电流分布，实际硬件（如超材料表面）如何高精度地实现这种连续分布仍是工程上的巨大挑战。

（完）
