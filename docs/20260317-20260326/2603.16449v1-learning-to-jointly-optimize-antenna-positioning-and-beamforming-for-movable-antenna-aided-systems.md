---
title: Learning to Jointly Optimize Antenna Positioning and Beamforming for Movable Antenna-Aided Systems
title_zh: 学习联合优化可移动天线辅助系统的天线定位与波束赋形
authors: "Yikun Wang, Yang Li, Zeyi Ren, Jingreng Lei, Yik-Chung Wu, Rui Zhang"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.16449v1"
tags: ["query:dl-comm"]
score: 10.0
evidence: 用于天线定位和波束成形的端到端深度学习框架
tldr: 本文研究可移动天线（MA）辅助的多用户下行传输系统，旨在解决天线位置与波束赋形的联合优化难题。针对天线位置离散性、波束赋形连续性及位置间耦合约束带来的挑战，提出一种端到端深度学习框架。该框架由定位模型和波束赋形模型组成，分别处理不同类型的变量与约束。实验证明，该方法在显著提升系统总速率的同时，大幅缩短了计算时间，为实时无线通信优化提供了高效方案。
selection_source: fresh_fetch
motivation: 旨在解决可移动天线系统中天线位置与波束赋形联合优化所面临的混合变量及复杂耦合约束挑战。
method: 提出一种端到端深度学习框架，通过定位模型处理离散变量与耦合约束，通过波束赋形模型处理连续变量。
result: 仿真结果表明，该框架在获得更高系统总速率的同时，显著降低了计算耗时。
conclusion: 深度学习是解决可移动天线系统复杂联合优化问题的有效途径，兼顾了通信性能与计算效率。
---

## 摘要
最近出现的可移动天线（MA）和流体天线技术通过在给定区域内动态调整发射或接收天线的位置，为增强无线系统的空间自由度提供了极具前景的解决方案。本文旨在解决 MA 辅助的多用户下行传输系统中天线定位与波束赋形的联合优化问题。该问题涉及混合的离散天线位置变量和连续波束赋形权重变量，以及天线位置上的耦合距离约束，这给优化算法设计带来了重大挑战。为了克服这些挑战，我们提出了一个端到端的深度学习框架，该框架由一个处理离散变量和耦合约束的定位模型以及一个处理连续变量的波束赋形模型组成。仿真结果表明，与现有方法相比，所提出的框架实现了更优的合速率性能，同时大幅减少了计算时间。

## Abstract
The recently emerged movable antenna (MA) and fluid antenna technologies offer promising solutions to enhance the spatial degrees of freedom in wireless systems by dynamically adjusting the positions of transmit or receive antennas within given regions. In this paper, we aim to address the joint optimization problem of antenna positioning and beamforming in MA-aided multi-user downlink transmission systems. This problem involves mixed discrete antenna position and continuous beamforming weight variables, along with coupled distance constraints on antenna positions, which pose significant challenges for optimization algorithm design. To overcome these challenges, we propose an end-to-end deep learning framework, consisting of a positioning model that handles the discrete variables and the coupled constraints, and a beamforming model that handles the continuous variables. Simulation results demonstrate that the proposed framework achieves superior sum rate performance, yet with much reduced computation time compared to existing methods.

---

## 论文详细总结（自动生成）

以下是对论文《Learning to Jointly Optimize Antenna Positioning and Beamforming for Movable Antenna-Aided Systems》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在可移动天线（MA）辅助的多用户下行通信系统中，如何**联合优化离散的天线位置选择与连续的波束赋形向量**，以最大化系统总速率。
*   **研究背景**：MA技术通过动态调整天线位置来改善信道状态。然而，该优化问题面临三大挑战：
    1.  **混合变量**：天线位置是离散的（从采样点中选择），而波束赋形是连续的。
    2.  **耦合约束**：天线之间必须保持最小距离（$d_{min}$），导致位置变量高度耦合。
    3.  **计算复杂度**：传统的组合优化或迭代算法（如Benders分解）计算量巨大，难以满足实时通信需求。

### 2. 方法论
论文提出了一个**端到端的深度学习框架**，由两个级联的神经网络（NN）组成：
*   **定位神经网络 ($F_p$)**：
    *   **编码器**：采用**边-节点图神经网络（EN-GNN）**，提取采样点与用户之间的信道特征，具有排列不变性（PI）和排列等变性（PE）。
    *   **解码器**：采用基于**多头注意力机制（MHA）和指针网络（Pointer Network）**的架构。通过**掩码（Mask）设计**，在每一步选择天线位置时自动过滤掉违反距离约束的采样点，确保输出始终合法。
*   **波束赋形神经网络 ($F_w$)**：
    *   利用**最优解结构**（基于拉格朗日对偶性），将复杂的波束赋形矩阵映射简化为预测少量的标量参数（功率分配和对偶变量），从而降低学习难度。
*   **联合训练算法**：
    *   由于位置选择是离散且不可微的，采用**策略梯度（Policy Gradient）**算法更新定位网络。
    *   波束赋形网络则通过标准的**随机梯度上升**进行无监督训练。

### 3. 实验设计
*   **场景设置**：考虑一个基站配备 $M$ 个可移动天线，服务 $K=4$ 个单天线用户。发射区域为 $2\lambda \times 2\lambda$ 的二维平面，离散采样点 $N$ 在 25 到 64 之间。
*   **信道模型**：采用场响应信道模型（Field-response channel model），考虑多径效应。
*   **对比方法（Benchmarks）**：
    1.  **Random+WMMSE**：随机选点 + WMMSE波束赋形。
    2.  **Strongest+WMMSE**：选择平均信道增益最强的点 + WMMSE。
    3.  **Strongest+ZF**：最强点 + 迫零波束赋形。
    4.  **FP-C**：一种现有的联合优化连续位置和波束赋形的迭代算法（经离散化投影处理）。

### 4. 资源与算力
*   **硬件**：使用了一块 **NVIDIA GeForce RTX 4090 GPU**。
*   **软件**：PyTorch 2.1.2。
*   **训练细节**：训练 100 个 epoch，每个 epoch 包含 50 个 mini-batch，batch size 为 1024。学习率设为 $10^{-4}$。

### 5. 实验数量与充分性
*   **实验规模**：
    *   测试了不同发射功率（0-30 dBm）下的性能。
    *   测试了不同天线数量（$M=4$ 到 $9$）和不同采样点密度（$N=25$ 到 $64$）的鲁棒性。
    *   测试集包含 1000 个独立样本。
*   **充分性评价**：实验涵盖了影响系统性能的核心变量（功率、天线数、采样点数），并对比了启发式算法与高复杂度的迭代算法。实验设计较为客观，通过消融式的参数变化验证了模型在不同约束下的有效性。

### 6. 主要结论与发现
*   **性能优越**：在所有测试场景下，所提 DL 框架的总速率均优于所有基线方法，尤其在高功率区域（干扰受限）表现更强。
*   **极速推理**：该方法的推理时间仅为 **7.89 毫秒**，而性能最接近的迭代算法（FP-C）需要 **3682.82 毫秒**。计算速度提升了约 460 倍。
*   **约束满足**：通过掩码机制，模型能够 100% 满足天线间的最小距离约束，无需任何后处理。

### 7. 优点
*   **端到端处理**：巧妙地结合了强化学习（处理离散变量）和深度学习（处理连续变量），解决了混合整数非线性规划（MINLP）难题。
*   **硬约束集成**：将复杂的空间距离约束直接集成到网络解码过程中，而非通过惩罚项（Soft constraint），保证了方案的可行性。
*   **领域知识驱动**：波束赋形网络利用了已知的最优解结构，显著缩小了搜索空间，提高了训练效率。

### 8. 不足与局限
*   **泛化性限制**：论文未明确讨论当用户数量 $K$ 发生变化时，模型是否需要重新训练（虽然 GNN 理论上支持变长输入，但解码器结构可能受限）。
*   **离散化误差**：该方法基于预定义的采样点，其性能上限受限于采样点的密度，无法像连续优化那样达到绝对的空间最优。
*   **信道状态信息（CSI）依赖**：假设采样点上的 CSI 是完全已知的，但在实际 MA 系统中，获取所有潜在位置的 CSI 具有极高的开销。

（完）
